[package]
name = "ide"
version = "0.1.0"
edition = "2021"
description = "The game's chrome — creative mode's toolbar + the CPU raster it draws with. Knows neither the game nor the GPU: rt-viewer's creative_host.rs feeds it plain data and composites the panels it returns."

[dependencies]
font8x8 = { version = "0.3", default-features = false }
