[package]
name = "house-game"
version = "0.1.0"
edition = "2021"
description = "The game's headless core (ARCHITECTURE.md area C): the town testbed world + the continuous-movement primitives — runs and tests fully headless (no GPU, no window)."

[dependencies]
sim-core = { path = "../sim-core" }
iso-core = { path = "../iso-core" }
glam = "0.30"
